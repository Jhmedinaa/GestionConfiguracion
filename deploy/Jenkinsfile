node{
  stage('SCM Checkout'){
    git 'https://github.com/Jhmedinaa/GestionConfiguracion'  
  }
  stage('Compile-Package'){
    def mvnHome = tool name: 'maven 3.5.4', type: 'maven'
    bat ${mvnHome}/bin/mvn "spring-boot:run"
  }
}
